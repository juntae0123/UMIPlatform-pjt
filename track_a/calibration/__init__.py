"""Camera calibration — NOT IMPLEMENTED. S15P21A103-28.
카메라 캘리브레이션 — **구현 없음.** S15P21A103-28.

내부 파라미터(초점거리·주점·왜곡)와 외부 파라미터(카메라↔로봇 베이스).

완료 기준은 판정 가능하게 쓴다. "잘 맞는다" 가 아니라 재투영 오차 몇 px 이내,
몇 장의 이미지로, 어떤 패턴에서 측정했는지까지.

⚠️ 캘리브레이션 오차는 이 태스크의 위치 허용오차 ±10mm 안에 들어와야 한다.
   근거는 AI/README.md 의 '측정 수치' 절 (replay 허용오차).

⚠️ 산출된 파라미터는 `AI/configs/` 로 간다. 코드에 하드코딩하지 않는다.

⚠️ 2026-09-08 추가 (LIMITS L40) — **계약이 이미지 비교가능성을 보장하지 않는다.**
   `EpisodeMeta.cameras` 는 이름 목록뿐이고 fovy·왜곡·마운트 pose 를 기록하는
   필드가 없다. 그래서 기하가 다른 두 데이터셋이 같은 이름으로 계약을 통과한다.
   시뮬 `cam_wrist` 는 fovy 70, 실물 폰은 미측정이다.

   ⚠️ 이 항목은 2026-09-09 에 **L61 로 번호가 바뀌었다** (L40 -> L61,
      LIMITS.md 「UMI 관통」 절의 재배정 기록 참조).

   ⚠️ 여기에 있던 처방("제3의 정규 가상 카메라로 양쪽 재투영")은
      **2026-09-09 철회했다** (REJECTED.md). 카메라 기하는 이미 결정된 사안이고
      계약의 카메라 필드 변경은 트랙 B 의 레인이 아니다.
      **한계 자체(L61)는 유효하게 남는다** — 철회한 것은 처방이다.
      실데이터에서 시뮬<->실물 이미지 분포 차이가 실패 원인으로 지목되면
      그때 계측을 들고 다시 온다.

   ⚠️ 계약 확장은 양 트랙 합의 + D-AI 기록 사안이다.
"""
