"""Pose to robot-frame transform — NOT IMPLEMENTED. S15P21A103-29.
pose → 로봇 좌표 변환 — **구현 없음.** S15P21A103-29.

⚠️ 좌표계 정의는 양 트랙에 영향을 준다. 축 방향·원점·단위를 바꾸면 D-AI 기록이 필요하다.

⚠️ 사람 손 궤적을 로봇이 재현하지 못할 수 있다. 사람 손목은 자유롭게 꺾이지만 로봇은
   5자유도 + 그리퍼이고, 위치(3) + 접근축(2) = 정확히 5개 구속이다.
   **6자유도 완전 자세 제어는 불가능하다.** 변환 결과가 로봇 관절 범위 안에
   들어오는지 검사하는 도구가 수집 착수 전에 있어야 한다 —
   시뮬 쪽 대응물은 `tools/reach_scan.py` 다.
"""
