"""Pose/video time synchronisation — NOT IMPLEMENTED. S15P21A103-30.
pose ↔ 영상 시간동기화 — **구현 없음.** S15P21A103-30.

이 프로젝트에서 추적이 가장 어려운 실패의 원인이다. 어긋나면 모델이 "과거 화면 보고
미래 행동"을 학습한다. **학습 손실은 잘 떨어지고 시뮬 성공률도 나오는데 실물에서만
실패한다.**

## ⚠️ 계약의 10ms 게이트는 이 오차를 보지 못한다 — 2026-09-08 추가

`contract/episode.py` 의 검증기는 `state_timestamp` 와 `action_timestamp` 의 차이를
10ms 이내로 검사한다. 그런데 **둘 다 pose 스트림에서 나온다.**
`umi/convert.py` 는 `state = IK(pose[t])`, `action = q[t+1]`, 타임스탬프는 둘 다
`pose_timestamp[t]` 로 둔다 — 그래서 그 게이트는 구조적으로 항상 통과한다.

**정작 재야 하는 것은 pose 스트림과 RGB 스트림 사이 오차**이고, 계약에 그 필드가 없다.
계약을 바꾸지 않고 `EpisodeMeta.notes["image_sync_offset_ms"]` 에 실측값을 넣는다.
`umi/convert.py` 가 변환할 때 스텝마다 재서 중앙값·p95·최대를 기록한다.

실측 예 🟢 (합성, 이미지 30Hz · 지연 8ms): 모든 스텝에서 정확히 8.00ms —
**랜덤이 아니라 상수 편향이다.** 평균으로 지워지지 않는다. 손 0.3m/s 가정 시 2.4mm 🟡.

## 리샘플 방향 — 픽셀은 보간하지 않는다

`umi/convert.py` 는 pose 타임스탬프마다 **가장 가까운** 프레임을 고르고 보간하지
않는다. 두 프레임을 섞으면 실제로 본 적 없는 이미지가 되고, 지어낸 픽셀로 학습한
정책은 모델 문제처럼 보이는 방식으로 실패한다.

그래서 남는 오차의 상한은 **이미지 주기의 절반**이다. 실측 확인 🟢: 이미지가
27.6Hz, pose 가 30Hz 일 때 최근접 오차 최대 17.95ms (이미지 반주기 18.09ms 이내).

**여기서 설계 결론이 하나 나온다 — pose 는 보간할 수 있고 이미지는 못 한다.**
그러므로 RGB 스트림을 pose 보다 **느리게 두면 안 된다.** 느리면 최근접만으로는
못 줄이고, 줄이려면 pose 를 이미지 시각으로 보간해야 한다(그건 정당하다).
어느 쪽을 마스터 클록으로 둘지가 이 이슈의 결정 사항이다.

**대책보다 계측기를 먼저 만든다.** 수집 착수 전에 오차를 재고, 그 수치를 기준으로
동기화 방식을 정한다. 시뮬에서는 오차가 구조적으로 0 이라 이 이슈를 검증했다고
볼 수 없다.
"""
