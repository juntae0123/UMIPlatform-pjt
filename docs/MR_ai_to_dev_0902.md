# MR 본문 — `ai` → `dev` (2026-09-02)

> GitLab MR 생성 시 아래 본문을 그대로 붙여넣는다.
> 링크: `/-/merge_requests/new?merge_request%5Bsource_branch%5D=ai`

---

**제목**: `[AI] ai → dev 병합 — 시뮬 계측 기반 구축, 데이터 계약 0.2.0, 스킬 5종`

## 무엇이 들어가는가

커밋 86건 · AI/ 아래 149파일 · 8/27 이후 AI 파트 작업 전부.
`dev` 가 8/28 이후 갱신되지 않아 밀려 있던 것을 한 번에 따라잡는다.

### 1. 시뮬 환경과 데이터 파이프라인 (S15P21A103-63, -64)

- MuJoCo SO-101 픽앤플레이스 씬. **공식 MJCF 는 수정하지 않고** 로드 시점에
  `MjSpec` 으로 카메라 2대와 그리퍼 접촉 패드를 주입한다 (업데이트 시 교체만 하면 된다)
- 자세구속 IK, 파지 시퀀스, 작업공간 도달성 지도
- 시뮬 시연 수집·검증 파이프라인, 대화형 뷰어, 텔레옵 녹화

### 2. 데이터 계약 (S15P21A103-27) — 두 트랙의 유일한 접점

- `contract/episode.py` 가 정본. 관측·행동·타임스탬프 규격 + 검증기 + 왕복 검증
- **계약 0.2.0**: `EpisodeMeta.skill_id` 필수, `contract/ids.py` 의 enum 강제
- 구버전(0.1.0) 에피소드는 **읽기는 되고 학습은 거부**된다 — 진단에는 그 데이터가
  필요하지만 학습에 섞이면 안 되기 때문
- ⚠️ 트랙 A 가 FE 우선순위 작업 중이라 김준태가 대행 결정했다 (D-AI-26).
  복귀 시 재확인 대상이며 LIMITS L36 에 올려뒀다

### 3. 정책·baseline·게이트 (S15P21A103-60, -34, -65)

- baseline 4종(hold/zero/replay/scripted)과 게이트 4종.
  **게이트 기준은 결과를 보기 전에 코드 상수로 고정**돼 있다
- BC 학습 루프, 롤아웃 채점 harness
- sim↔실물 교체 가능한 정책 인터페이스 (시뮬레이터 없는 더미 환경으로 검증)

### 4. 진단 계측기 5종 (S15P21A103-34, -65)

| 도구 | 답하는 질문 |
|---|---|
| `diagnose_bc` | 손실이 낮은 이유가 학습인가, 자명한 함수로 근사되기 때문인가 |
| `image_sensitivity` | 정책의 행동이 실제로 보는 것에 반응하는가 |
| `success_map` | 정책이 작업공간의 어디에서 되는가 |
| `check_domain` | 도메인 랜덤화가 실제로 흔드는가 (계측기 자체 검증) |
| `measure_transfer` | 도메인이 바뀌면 얼마나 무너지는가 |

### 5. 스킬 레지스트리 + VLM 출력 스키마 (S15P21A103-109)

- 스킬 5종 확정, `tier`(tutorial/demo)·`status` 구분
- **게이트 없는 체크포인트는 배포 불가**로 판정한다 (D-AI-23)
- VLM 출력은 enum 제약 + 기권 게이트 (D-AI-24)

### 6. 기록 (S15P21A103-27 외)

MEASURE 5건 · TS 8건 · DECISIONS D-AI-1~27 · DEVLOG · KPT ·
`LIMITS.md`(미검증 36건) · `REJECTED.md` · BE 전달 스키마 문서

## 검토 시 봐줬으면 하는 것

- `contract/` — 두 트랙 접점이다. 여기가 바뀌면 수집한 데이터가 무효가 된다
- `configs/so101.yaml` — 하드웨어 의존 값 단일 출처. 코드에 하드코딩된 값이 없어야 한다
- 게이트 상수들 (`eval/rollout.py` `GATES`, `contract/skills.py`) — 결과를 보고 고치면
  판정이 아니라 사후 합리화가 된다

## 아직 안 되는 것 (숨기지 않고 적는다)

- **게이트를 넘은 학습 정책이 없다.** BC 롤아웃 0% 이고 원인은 규명됐다 —
  절대 관절각 목표에서 상태가 정답의 86% 를 설명해 이미지가 쓰일 이유가 없었다.
  대책(델타 행동)은 미검증
- 실물 데이터 0편. 모든 수치는 시뮬이며 실물을 보장하지 않는다
- VLM 은 fp16 안전성만 통과. 파인튜닝·선택 미완
- n=20 계측의 신뢰구간 반폭이 ±18%p 다. 게이트 판정에는 n=100 이 필요하다

## 영향 범위

`AI/` 만 변경한다. BE·FE·ROS·HW 파일은 건드리지 않았다.
