# MEASURE — 관측 절제 (폐루프) 2026-09-11

- 확신도: 🟢 실행·로그 확인
- 사전등록: `docs/PREREG_obs_ablation_0911.md` (착수 전 기록)
- 도구 `tools/probe_obs_ablation.py` sha `848469decd174da2` · git `c3dd48e`
- 조건: n=100 · seeds 3000~3099 · 기증자 시드 2999(평가 블록 밖) · render ·
  policy-device cpu · jitter ±50mm · `max_ticks=200` · 최초 성공 시 종료
- **`full` 재현 fixture 3/3 PASS** (6 · 69 · 25) 🟢

## 결과

성공률 (100편 중) / 실패 포함 최근접 거리 중앙값

| 조건 | seed0 | seed1 | seed2 |
|---|---|---|---|
| `full` | **6** · 3.7mm | **69** · 2.2mm | **25** · 2.3mm |
| `donor_both` | 0 · 44.4mm | 10 · 37.1mm | 0 · 27.2mm |
| `donor_front` | 3 · 15.8mm | 55 · **1.9mm** | 12 · 6.7mm |
| `donor_wrist` | 0 · 47.4mm | 6 · 16.4mm | 0 · 23.5mm |
| `freeze_both` | 0 · 22.6mm | 0 · 24.5mm | 0 · 25.4mm |
| `blank_both` | 0 · 93.9mm | 0 · 163.0mm | 0 · 135.1mm |

`donor_both` 요약

| | 절대 하락 | 상대 상실 | 최근접 배율 |
|---|---:|---:|---:|
| seed0 | +6.0%p | **100%** | 12.0배 |
| seed1 | **+59.0%p** | 86% | 16.9배 |
| seed2 | +25.0%p | **100%** | 11.8배 |

## 판정 — "관측 미사용" 가설 기각 🟢

주 판정 기준(`donor_both` 하락 >= 50%p → 이미지를 쓴다)을 **seed1 이 +59.0%p 로 통과**한다.
나머지 둘은 절대값이 작지만 **상대 상실 100%** 이고 최근접이 **12배** 멀어진다.

**정책은 이미지를 확실히 쓴다.** 따라서 **팔 정밀도가 진짜 병목**이라는 해석이 유지되고,
행동 표현·손실 가중·데이터 쪽 작업을 계속할 근거가 있다. 여기서 "안 쓴다"가 나왔으면
D-AI-38 이후 우선순위를 전부 다시 세워야 했다.

## ⚠️ 도구 결함 — 판정 함수가 바닥 효과를 못 걸렀다 (정정함)

초판 `verdict()` 는 **절대 하락폭만** 봤다. seed0 은 `full` 이 6/100 이라 최대 하락이
6%p 이고, 그래서 `images_barely_used` 가 찍혔다 — **실제로는 100% 를 잃었다.**

사전등록 예측 4번에 "seed0 은 바닥 효과 때문에 절대 %p 로 읽는다"고 적어놓고
**판정 함수에 반영하지 못한 것이 원인이다.** 같은 커밋에서 고쳤다 —
이제 절대 하락폭 · **상대 상실률** · **최근접 거리 배율** 세 가지를 함께 보고,
`full` 이 15% 미만이면 `inconclusive__floor_effect` 를 낸다.

**출력된 verdict 문자열 중 seed0 의 `images_barely_used` 는 무효다.**

## 카메라별 기여 — 손목이 압도적 🟢

seed1 기준 (`full` 69/100 이라 바닥 효과가 없는 유일한 체크포인트):

```
donor_front  69 → 55  (−14%p)   최근접 2.2 → 1.9mm
donor_wrist  69 →  6  (−63%p)   최근접 2.2 → 16.4mm      4.5배
```

보조 판정 2번(wrist 하락이 front 의 2배 이상) **통과.**
카메라 배치·hand-eye 캘리브레이션 우선순위는 **손목**이다.

⚠️ 도구가 seed0 에서 "`cam_front` 기여 없음(3.0%p)" 경고를 찍었으나 **바닥 효과다.**
seed1 14%p · seed2 13%p 로 기여가 있다. **L62(카메라 2대 요구)에 "front 불필요"로
쓰지 않는다.** 다만 비중이 wrist 의 약 1/4 이라는 것은 실측이고 2대 구성 논의의 근거다.

## 그 외

- `freeze_both` 가 `donor_both` 보다 **더** 떨어진다 (seed1: 0 vs 10). 예측 적중 —
  정책은 내용뿐 아니라 **시간에 따른 변화**도 쓴다
- `blank_both` 는 최근접 94~163mm 로 완전히 붕괴한다. **분포 밖이므로 이것으로
  "이미지를 쓴다"고 결론 내지 않는다** (보조 판정 4번). 보조 읽기로만 남긴다
- seed1 `donor_front` 의 최근접이 **1.9mm 로 `full`(2.2mm)보다 가깝다**. 접근은 되는데
  성공률은 69→55 로 떨어진다 — 깨진 것이 접근이 아니라 다른 단계(폐쇄 시각 추정)일
  가능성 🟡. **미검증.** 별도 계측 대상

## 예측 대조

| 예측 | 실측 | |
|---|---|---|
| `donor_both` 하락 30~60%p (부분 사용) | seed1 +59 / seed0 +6 / seed2 +25 | ✓ (seed1), 나머지는 바닥 효과 |
| `donor_wrist` 하락이 `donor_front` 의 2배 이상 | **4.5배** | ✓ |
| `freeze_both` 가 `donor_both` 보다 더 떨어진다 | 0 vs 10 | ✓ |
| seed0 은 절대 하락폭이 작다 — %p 로 읽어라 | +6.0%p | ✓ (그런데 **도구가 그걸 못 지켰다**) |

## 해석 제한

- 시뮬이다. 실물 전이를 보장하지 않는다
- 기증자 이미지는 그 상태와 함께 나타난 적 없는 조합이다. **절대값을 물리량으로 읽지 않는다**
- 카메라 기여도는 **시뮬 배치 기준**이다. 실물 마운트 확정 시 재측정 대상
- 체크포인트 3개. 학습 실행 간 25%p 편차 실측이 있다 🟢
- 바닥 효과 때문에 **seed1 이 사실상 유일한 해석 가능 체크포인트**다. 나머지 둘은
  방향만 확인해준다
