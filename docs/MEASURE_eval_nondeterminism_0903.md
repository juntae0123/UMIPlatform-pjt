# MEASURE · 롤아웃 평가의 비결정성 (같은 ckpt 3회)

작성: 김준태(트랙B) · 2026-09-03 · 관련 이슈 S15P21A103-35 · LIMITS L37
환경: V100 서버, MuJoCo 3.12.0, `MUJOCO_GL=egl`, git `ad4907e` (dirty=True)
명령: `python tools/eval_rollout.py --episodes 100 --seed-base 3000 --render --policy-ckpt checkpoints/bc/sim_pick_v2_seed0.pt --log`
**세 번 모두 완전히 같은 명령·같은 체크포인트·같은 시드 블록(3000~3099)**

**판정: 이미지를 입력으로 쓰는 정책의 롤아웃 성공률이 재현되지 않는다. 원인은 미확정.**

## 결과 🟢

| 정책 | 이미지 사용 | 1회 | 2회 | 3회 |
|---|---|---|---|---|
| hold | 안 씀 | 0/100 | 0/100 | 0/100 |
| zero | 안 씀 | 0/100 | 0/100 | 0/100 |
| scripted | 안 씀 (특권 정보) | **84/100** | **84/100** | **84/100** |
| **bc** | **씀** | **11/100** | **10/100** | **7/100** |

`scripted` 는 실패 형태 통계까지 소수점 단위로 동일했다 (0.3mm · Q1 0.2 / Q3 0.3 · 턱 접촉 94% · 그리퍼 −0.756).

**이미지를 쓰지 않는 정책 셋은 완전히 재현되고, 쓰는 하나만 흔들린다.**
→ 원인은 이미지 경로(렌더 또는 추론)에 있다. 씬·물리·시드는 결정적이다.

## 수치의 의미

- seed0 체크포인트 합산 **28/300 = 9.3%** · Wilson 95% **6.6~13.0%**
- **평가만으로 범위 4%p** (7~11), n=100 기준
- 2026-09-02 기록 "학습 실행 간 폭 9%p (11 / 2 / 4%)"는 **학습 분산과 평가 분산이 섞인 값이다.**
  다만 2%·4% 는 7~11 범위 밖이므로 학습 분산도 실재한다. **둘 다 있고 분리 보고가 필요하다.**

## 재현되는 것 — 실패 형태

| 지표 | 1회 | 2회 | 3회 |
|---|---|---|---|
| 파지점-물체 최소 수평거리 중앙값 | 6.1mm | 5.8mm | 6.0mm |
| ≤15mm | 73% | 72% | 73% |
| 턱 접촉 | 85% | 84% | 83% |
| 그리퍼 명령 최솟값 중앙 | −0.784 | −0.784 | −0.784 |

**성공 건수는 흔들리는데 형태 통계는 안 흔들린다** (표본 90건). "가까이 가서 못 잡는다"는 진단은 유효하다.

## 소급 적용

EXP_LOG 에 기록된 **모든 `bc` 롤아웃 수치**에 이 한계가 적용된다. 단일 실행 수치로 체크포인트를 비교한 기록이 있으면 무효다. 0% 였던 실행들은 영향이 없다 (0 은 0 이다).

## 조치

1. **프로토콜**: 롤아웃 수치는 같은 ckpt **3회 평가 + 합산 + 범위** 로 보고한다
2. `repeat_runs` 에 학습 반복과 평가 반복을 분리해 넣는다 (미구현)
3. 원인 좁히기 — `check_determinism` (미구현): 같은 씬 두 번 렌더 비트 동일? · 같은 관측 두 번 같은 행동? · 같은 시드 롤아웃 두 번 같은 결과?
   후보: EGL 렌더 비결정성 🟡 / torch CPU 추론 스레드 스케줄 🟡. **둘 다 미검증**
