# KPT 2026-09-02

작성: 김준태(트랙B)

## Keep

- **원인을 셋으로 쪼개는 계측기를 먼저 만들었다** (`trace_execution` A/B/C). 한 번 돌려서 원인이 특정됐다
- **클라우드 컨테이너에서 end-to-end 자가검증** 후 서버에 올렸다. 서버 트라이 2번으로 끝났다
- 계측기에서 처방 문구를 뺐다. 판정만 남긴다
- 첫 폐루프 성공(1편) → 첫 0 아닌 성공률(5.7%, n=300, 3회) — 둘 다 조건 병기해서 기록

## Problem

- **어시스턴트**: `run_epoch` 학습 호출에 표준화 인자를 빼먹었다 (선택 키워드 → 위치 인자에 밀림). 4번째 결함이자 2번 수정이 만든 결함
- **어시스턴트**: "train 0.7% = 외웠다" — 단위가 다른 두 수를 비율로 비교했다. 손실에 단위 표기가 없었다
- **어시스턴트**: image_sensitivity 게이트가 절대 분모를 써서 이미지를 보는 정책에 "실패"를 찍었다
- 외우기 검사에 홀드아웃 28개가 섞여 있었다 (val_fraction 0.2 기본값)
- 그리퍼 델타의 88% 가 0 인 것을 데이터를 찍어보기 전까지 몰랐다 — 학습 전에 목표 분포를 한 번 봤어야 했다
- 하루에 원인 설명이 5번 바뀌었다. 매번 "맞았지만 불완전"이었다

## Try

- [ ] 손실 변환·단위처럼 **의미를 바꾸는 것은 선택 인자로 두지 않는다** (필수 객체)
- [ ] 학습 전에 회귀 목표의 관절별 분포(0 비율·std)를 출력한다 — 그리퍼 같은 스파이크 목표를 미리 본다
- [ ] 계측기는 판정만. 처방은 판정 뒤 사람이 낸다
- [ ] 서버 명령은 클라우드에서 같은 데이터로 먼저 돈 것만 넘긴다
- [ ] ① 접근 정밀도 계측기 → ② 데이터 스케일링 98→400 → 그 결과로 모델/데이터 결정
- [ ] val 은 episode 단위 분할로 바꾼다. 지금 val 은 일반화를 안 잰다
