# KPT 2026-09-07 · 김준태(트랙B)

**한 줄:** 성공률은 0%p 올랐다(9.3% 그대로). 틀린 믿음 6개를 걷어냈고 계측기 7개를 고쳤다.

## Keep

- **원장 자동 생성** (`FINDINGS.md`, EXP_LOG 에서 렌더). 결정 시점에 이전 결과가 눈앞에 뜬다.
  v2·v3·v5 가 같은 데이터라는 것을 이걸로 잡았다
- **사전등록된 분기 규칙이 실제로 발동했다.** "동결 < 30% 면 계열 하향" 을 결과 전에 박아두고,
  11% 가 나와서 그대로 내렸다. 결과 보고 정하지 않았다
- **판정 통계를 모집단으로 좁힌 것.** 전체 성공률로는 안 갈렸고 얼어붙은 편만 짝지으니 갈렸다
- **연기시험이 판정 전에 버그를 잡았다.** 7건 전부 수치를 쓰기 전에 걸렸다
- **회신에 산수를 같이 적은 것.** 마커 26px 어림을 🟡 로 표기하고 계산을 붙였더니 MW 가
  전제(마커 거리)를 짚어 반박했다. 결론만 적었으면 ±3.5mm 가 스펙에 남았다

## Problem

- **계측기 버그 7건.** 전제 위반(`closing_moment`) / 측정 시점(턱접촉·물체밀림·상승량 2건) /
  개입 무효(kick 이 IK 에 막혀 무동작) / 지표 선택(페어링을 shift 무관 지표로 판정).
  공통 뿌리 하나 — **"무엇을 재는가"를 검증하지 않고 "얼마가 나오는가"로 갔다**
- **철회 3건을 문서·LIMITS·DEVLOG·포트폴리오까지 쓴 뒤에 철회했다.** 실제 잘못은
  섞은 게 아니라 **추론에 🟢 를 붙인 것**이다. 🟡 였으면 팀에 올라가지 않았다
- **서버 규약 위반.** GPU 두 장 점유, 스레드 폭발(load 165/80), 같은 잡 4중 실행.
  전부 손으로 조립한 명령줄에서 나왔다. 서버 측 경고를 받았다
- **rebase 진행 중에 커밋해서 detached HEAD.** 커밋 전 `git status` 를 안 봤다.
  커밋 메시지 하나를 잃었다(내용은 보존)
- **5일치 "데이터 조건 실험"이 실은 같은 데이터였다.** 데이터셋 이름을 근거로 인용했다.
  실제로 데이터를 바꾼 것은 v4(dwell) 하나뿐
- **DEVLOG 를 빼먹어서 지적받고 썼다**

## Try

1. **기전 주장은 무조건 🟡.** 다른 계측기나 사전등록된 실행이 동의할 때까지.
   🟢 에는 근거 계측기 이름을 병기한다
2. **숫자를 읽기 전에 "무엇이 이 값을 틀리게 만들 수 있나"를 먼저 적는다.** 못 적으면 해석하지 않는다
3. **조건 비교는 커밋된 트리에서만** (D-AI-31). 표준 블록 5시드, 최종 후보 10시드 (D-AI-30).
   `runtime_limits` 가 GPU 한 장·스레드 캡·중복 방지를 강제한다
4. **다음 계측 순서: A′ → T3 → T4.** 실패 89/100 중 동결은 11 뿐이므로 접근 단계가 주력이다.
   Z1(정지 창 재표집)은 후순위 — 기여 상한이 6%p 다
5. **이슈 113 착수** (IK 실행가능성 검사기). MW 실물 그리퍼 데이터가 왔고 (a)(c) 를 못 냈다
6. **L59 pose 원점 문제를 트랙 A(도경)에 올린다.** 좌표 변환이라 그쪽 소관이고,
   해결 없이는 관절각 변환이 불가능하다

## 오늘 확정된 수치 (전부 조건 병기, 🟢)

| | |
|---|---|
| v2·v3·v5 데이터 | state+action 해시 동일. v3≡v5 는 이미지까지 동일 |
| v6 이미지 증강 | 12/5/0 = 5.7%, 합산 17/300, v5 와 구간 겹침 → R-AI-14 기각 |
| 시연 정지 창 | 22틱(0.73초), Q1=Q3=22, 98편 전부 |
| 시연 들기 | 98/98 성공, 잡고 못 든 편 0/98 |
| 천장 손실 | 8cm 명령의 IK실패 15 ↔ 실패 16. 7.0cm 로 84%→99% (여유 13.1mm) |
| 달성 상승량 | 명령의 87~91% 로 단조 (5.5→48.1 / 7.0→63.1 / 8.0→73.2mm) |
| BC 동결 | 11%뿐. 그중 7/11 은 턱접촉 0(공중에서 닫음). 실제 파지 후 동결 4% |
| T0 배포 경로 | round_trip 1.86e-09, rollout_match 20/20 → 디코드 깨끗 |
| 스레드 vs 장치 | 스레드 1==8 동일. cpu 11/100 vs cuda 12/100 → 장치가 원인 |
| UMI 실물 궤적 | 발자국 12.5×29.1cm(도달 15×35 안), 수평축 회전 최대 7.2°(12° 이내 0/306) |
