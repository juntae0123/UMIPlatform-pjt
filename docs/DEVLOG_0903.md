# DEVLOG 2026-09-03

작성: 김준태(트랙B)

## 한 줄

**성공률은 오르지 않았고, 대신 성공률을 믿을 수 있게 됐다.** 그리고 실패 원인이
처음으로 하나로 좁혀졌다 — 정책은 8~12mm 까지 가서 **15~24mm 에서 그리퍼를 닫는다.**

## 오늘 확정된 것 🟢

| 항목 | 값 | 조건 |
|---|---|---|
| v3 새 기준선 | **13/300 = 4.3%** (9 / 3 / 1%), 95% 2.5~7.3% | 결정적 렌더, episode 분할, 30 epoch |
| v2 와의 관계 | **구간 겹침** (v2 3.6~8.9%) — 성능 불변 | MSAA 제거는 성능을 안 바꿨다 |
| 학습 분산 | **폭 8%p 전부** (평가 분산 0) | 최고 9% / 최저 1% = 9배 |
| 닫는 순간 거리 | bc **15.7 / 16.1 / 23.6mm** vs scripted **4.7mm** | 실측 허용오차 ±15mm = 1/4 |
| 최근접 → 닫는 순간 | **+6.3 ~ +11.7mm 발산** | 접근은 하고 유지를 못 한다 |
| 렌더 비결정성 원인 | **MSAA (`offsamples=4`)** | 0 으로 끄면 R·P·E 비트 동일 |
| 1계조 민감도 | 성공/실패 뒤집힘, 닫은틱 74→37 | **L39 — 정책 강건성 문제** |
| val 순위 (episode 분할) | **3/3 일치** (0.0533/0.0572/0.0694 → 9/3/1%) | n=3, 우연 17% — 확정 아님 |
| 목표 분포 (98편) | pan zero **56%**, roll **68%** | 2편 표본 관찰이 확인됐다 |
| seed2 이상 | 턱접촉 **7%**, 닫은틱 **44** | 질적으로 다른 실패 양식 |

## 오늘 좁힌 결함 4건 (TS_render_and_demonstrator_0903)

1. **렌더 비결정성 → MSAA.** baseline 셋이 재현되고 bc 만 흔들린 것으로 이미지 경로를
   좁히고, R/P/E 분리로 렌더를 지목하고, MSAA 를 껐다. L37 해소 / L39 신규
2. **상한선이 시연자와 달랐다.** 파지 계획이 `collect.py`(141틱)와
   `baselines.py`(165틱) 두 곳에 복제돼 갈라져 있었다. `pick_waypoints` 로 합쳤다
3. **읽는 규칙 오류.** `지연 >5틱 → 타이밍` 이 scripted(+30틱, 84%)를 오분류한다.
   판정을 닫는 순간의 절대 거리로 옮겼다
4. **`--check` 가 낡은 로그로 통과.** README 생성 블록에 리비전·HEAD 대조를 넣었다

## 오늘 만든 것

- `eval/determinism.py` — R/P/E 분리, 비트 단위 일치만 통과
- `closing_moment` — 닫는 시점·그때 거리·최근접 대비 지연. 문턱은 에피소드 자신의
  그리퍼 명령 최대·최소 중간값 (하드코딩된 그리퍼 값에 의존하지 않는다)
- `cameras.offsamples` — 렌더 결정성 노브를 설정으로
- `sim/mujoco/kinematics.py: pick_waypoints` — 시연자 정의 하나로 통합 + `grasp.timing`
- `grasp.dwell_s: 0.5` — 파지점에서 열린 채 15틱 유지. **재측정 대기**
- `tools/readme.py freshness()` — 로그 최신성 가드
- 문서: MEASURE 3건, RULE(데이터 보관), EXPLAIN(발표자용 시뮬 역할), MODELS,
  HANDOVER(데이터 총정리), D-AI-29 + 개정 1차, R-AI-7~11, LIMITS L37 해소·L38·L39

## 오늘 확정되지 않은 것

- **dwell 효과** — v4 재수집·재학습 대기. 1차 지표는 닫는 순간 거리(<10mm), 2차가 성공률
- **시연자 통합의 영향** — 165틱 baseline 으로 잰 `scripted 84%`·`4.7mm` 가 141틱과
  같은지 미확인. v4 실행의 scripted 값으로 확인한다 🟡
- **L39 정책 강건성** — 하니스를 고친 것이지 정책을 고친 게 아니다
- **val 순위상관** — n=3 으로는 R-AI-8 을 되살릴 수 없다. 6회 이상 필요
- **pan 스파이크 목표** — 56% 가 정확히 0. 그리퍼(D-AI-28)와 같은 구조, 처방 미정
- **D-AI-29 G1 값** — VLA 대조군일 때 +20%p 유지할지 별도값 둘지 **김준태 결정 대기**
- EXP_LOG 서버 → 저장소 커밋. **오늘 수치 전부가 서버 디스크 한 곳에만 있다**

## 팀

- **신현우(PM/MW)**: D-AI-29 지적 3건 수용 → 개정 1차. 평가 조건 홀드아웃 조항은
  **원안의 구멍이었다** (어시스턴트 누락). 발표자용 시뮬 역할 설명 문서 전달
- **발표**: "UMI 학습 / AI 학습" 이 아니라 **데이터 계약**을 경계로 나눌 것을 제안.
  시뮬은 학습 데이터가 아니라 ①실행가능성 검사 ②평가 하니스 ③사전학습 세 지점 개입
- **PM**: 수익성 근거 메모 전달 (인력 대체 / 티칭 교체 / 구독, 출처 11건)
- **황도경(트랙A)**: AI 데이터 총정리 전달. 확인 필요 3건(그리퍼 신호·정면 카메라·hand-eye)

## 내일

1. v4 수집(156틱) → `verify_dataset` → `repeat_runs --runs 3`. **닫는 순간 거리가 1차 지표**
2. v4 의 `scripted` 값 확인 — 시연자 통합의 영향
3. EXP_LOG 서버 커밋 + `update_readme.py` 재실행
4. dwell 이 효과 없으면 → pan 목표 처리 또는 SmolVLA V1(V100 에서 도는지)
5. D-AI-29 G1 값 결정
