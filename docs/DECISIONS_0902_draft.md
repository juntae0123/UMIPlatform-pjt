# DECISIONS 초안 — 2026-09-02

작성: 김준태(트랙B). DECISIONS.md 병합 전 초안이다. 관련 이슈 S15P21A103-109.

---

## D-AI-22 · 스킬 5종 확정, 구조는 [집기=학습] + [놓기=스크립트]

작성자: 김준태(트랙B)

**결정 — 튜토리얼 스킬 5종**

⚠️ **이 다섯은 "우리가 시연할 다섯"이 아니다.** 플랫폼에 미리 학습시켜 넣어두는
   범용 프리미티브, 곧 **튜토리얼 층**이다. 시연에서 보여줄 것은 특정 페르소나를
   겨냥한 **데모 층** 스킬 1~2개이며 아직 미정이다 (페르소나 확정 대기).

   레지스트리에 `tier` 와 `status` 를 두는 이유가 이것이다. 제품 주장은
   "우리가 다섯 행동을 만들었다"가 아니라 **"사용자가 시연으로 새 행동을 추가할
   수 있다"** 이고, 그 주장을 증명하는 것은 시연 자리에서 추가된 스킬 하나다.
   다섯이 다 구현되지 않을 수 있다는 것을 전제로 둔다.

| `skill_id` | 이름 | 놓기 단계 | VLM |
|---|---|---|---|
| `pick_place` | 집어 옮기기 | 지정 좌표로 이동 후 개방 | — |
| `sort_two` | 두 곳으로 분류 | 분기된 트레이로 이동 후 개방 | 필요 |
| `align_fixture` | 지그에 정렬 | 지그 좌표로 이동, 자세 정렬 후 개방 | — |
| `present_inspect` | 검사 자세 제시 | 제시 자세로 이동, 회전, 복귀 후 개방 | — |
| `line_up` | 순서대로 늘어놓기 | 인덱스로 계산된 좌표로 이동 후 개방 | — |

**결정 — 구조**

모든 스킬은 `[집기 = 학습 정책] + [놓기 = 스크립트]` 로 분할한다.

**채택안 A** — 파지 정책 **하나**를 다섯 스킬이 공유하고 놓기 단계만 다르다.
**확장 목표 B** — 파지 정책이 게이트를 넘으면 스킬별 독립 정책 5개로 확장한다.

**왜 이 분할인가** 🟢
취향이 아니라 계측에서 나왔다. 기록된 궤적의 재생 성공률이
±5mm 4/4 · ±10mm 3/4 · ±15mm 1/4 · ±50mm 0/4 다 (2026-08-27, MuJoCo 3.12.0).
즉 물체 위치가 변하는 구간(집기)에는 정책이 필요하고, 물체가 그리퍼에 들어온
뒤(놓기)에는 필요 없다.

**왜 A 부터인가**
어느 안이든 파지 정책 **하나가 먼저 게이트를 넘어야 한다.** 하나도 못 하는데
다섯은 못 한다. 2026-09-01 기준 BC 는 롤아웃 0% 로 게이트 미통과 상태다.

**정직성 장치**
`contract/skills.py` 의 `shared_policy_report()` 가 레지스트리를 읽어
"서로 다른 학습 정책이 몇 개인가"를 항상 사실대로 출력한다. 안 A 상태에서는
"5개를 학습시켰다"가 아니라 "파지 정책 1개를 5개 작업이 공유한다"로 말하라고
스스로 경고한다. 발표에서 뒷받침할 수 없는 주장을 하지 않기 위한 것이다.

**삽입(insertion)은 제외한다.** 그리퍼 간격 1.7cm 에 ±10mm 재현 붕괴로는
공차 관리가 불가능하다. REJECTED 행.

**되돌릴 조건**
- 파지 정책이 게이트를 넘고 데이터·GPU 여유가 확인되면 B 로 확장
- 스킬마다 접근 자세가 달라 공유 정책의 성공률이 스킬별로 20%p 이상 갈리면
  그 스킬은 독립 정책으로 분리

**미검증**
- 물체를 쥔 뒤 그리퍼 안에서의 자세 편차. 정밀 배치가 필요한
  `align_fixture` 가 이 값에 의존한다. 미측정
- **데모 층 스킬이 무엇인지** — 페르소나 확정 대기. 튜토리얼 5종과 다른 동작일 수 있다
- 다섯 중 몇 개가 실제로 게이트를 넘을지. `status` 필드가 항상 사실대로 말한다

---

## D-AI-23 · 스킬 레지스트리는 게이트 없는 정책을 거부한다

작성자: 김준태(트랙B)

**결정**
레지스트리 항목의 `policy.gate` 는 **필수 필드**다. 없으면 배포 불가로 판정한다.
게이트 기록은 다음을 모두 만족해야 한다.

```
metric == "rollout_success"
n     >= 100      값 자체가 아니라 판정 가능성의 조건이다
runs  >= 3        학습 간 분산이 25%p 라 단일 실행은 체크포인트를 대표하지 못한다
value  > 0.20
ci95[0] > 0.20    신뢰구간 하한도 게이트 위여야 "통과"다
```

**왜**
게이트가 선택 필드이면 "학습이 끝났다"가 조용히 "된다"로 바뀐다.
`n >= 100` 과 `runs >= 3` 은 2026-09-01 실측에서 나왔다 — n=20 의 CI 반폭이
±18%p 로 게이트 폭과 같았고, 동일 데이터·설정의 학습 2회가 25%p 갈렸다.

**되돌릴 조건**
없다. 완화 제안이 나오면 게이트를 낮추는 대신 n 을 올리는 쪽을 먼저 검토한다.
게이트 변경은 사후 합리화, n 증가는 정밀도 개선이다.

---

## D-AI-24 · VLM 출력은 enum 제약 + 기권 게이트

작성자: 김준태(트랙B)

**결정**

```json
{
  "skill_id": "pick_place | sort_two | align_fixture | present_inspect | line_up",
  "target": {"description_ko": "...", "bbox_norm": [x0, y0, x1, y1]},
  "destination": "left_tray | right_tray | fixture | origin | target_pose | null",
  "confidence": 0.0,
  "abstain": false
}
```

- `skill_id` 와 `destination` 은 **constrained decoding** 대상이다. 자유 생성 금지
- 스킬마다 허용 `destination` 이 다르다 (`CATALOG` 가 정본)
- **`confidence < 0.70` 이거나 `abstain` 이면 팔을 움직이지 않고 사용자에게 되묻는다**

**왜 제약하는가**
자유 생성 모델은 그럴듯한 여섯 번째 스킬 이름을 만들어낸다. 하류에서 조용히
빠져나가는 `if` 문이 붙으면, UI 는 동작 중이라고 표시하는데 로봇은 아무것도 하지
않는 상태가 된다. `parse_vlm_decision()` 은 enum 밖의 값에 예외를 던진다.

**되돌릴 조건**
`0.70` 은 🟡 잠정값이다. M0(파인튜닝 없는 베이스 모델의 스킬 선택 정확도, n=100)
측정 후 재조정하되, **재조정했다는 사실과 이유를 반드시 기록에 남긴다.**

---

## 이 결정들이 다른 파트에 미치는 영향

⚠️ **트랙 A (황도경) 스키마 대기** — 데이터 계약에 `skill_id` 필드를 추가해야 한다.
   트랙 A 가 스키마를 주면 그때 반영한다.
   `EpisodeMeta` 에 `skill_id: str` 을 넣는 형태를 제안한다. 데이터 포맷 변경이라
   합의 없이 진행하지 않는다. **아직 `contract/episode.py` 를 고치지 않았다.**

✅ **HW 확인 완료 (2026-09-02)** — "6축"은 모터 6개를 뜻한다 = 팔 5축 + 그리퍼 개폐.
   실측·계약과 일치한다 (LIMITS L31 해소). **팔의 공간 자유도가 5인 것은 확정**이므로
   사람 손목 6DOF 궤적의 재현 불가 비율은 여전히 실측 대상이다 — 이슈 113 의 중요도가 올라간다.

⚠️ **BE (최은찬) 전달** — 배포 API 는 `gate` 없는 스킬을 거부해야 한다.
   유니크 인덱스는 `(robot_id, skill_id) WHERE status='active'` 로 정정.

⚠️ **이슈 114(스킬 5종 병렬 학습 잡 러너) 재검토** — 안 A 에서는 학습할 정책이
   하나뿐이라 병렬 잡 러너가 필요 없다. B 로 확장할 때 되살린다.


---

## D-AI-25 · 스킬을 튜토리얼 층과 데모 층으로 나눈다

작성자: 김준태(트랙B)

**결정**

| 층 | 무엇 | 개수 | 상태 |
|---|---|---|---|
| `tutorial` | 플랫폼 기본 제공 범용 프리미티브 | 5 | 전부 `planned` |
| `demo` | 특정 페르소나 겨냥, 시연에서 추가해 보이는 동작 | 1~2 | **미정 — 페르소나 확정 대기** |

레지스트리 항목은 `tier` 와 `status`(`planned`/`collecting`/`training`/
`gate_failed`/`deployed`)를 반드시 갖는다. `demo` 층은 `notes.persona` 가 없으면
검증에 실패한다 — 누구를 겨냥한 동작인지 없이는 시연에 쓸 수 없다.

**왜**
"5개를 학습시킨다"는 계획이고 다 구현되지 않을 수 있다. 계획된 개수를 구현된
개수처럼 말하는 것을 막아야 한다. `shared_policy_report()` 가
`실제로 동작하는 스킬은 N개다` 를 항상 출력한다.

더 중요한 것은 제품 주장의 위치다. 튜토리얼 다섯이 동작하는 것보다 **시연 자리에서
하나가 추가되어 동작하는 것**이 "사용자가 새 작업을 추가할 수 있다"는 주장의 더
강한 근거다.

**되돌릴 조건**
페르소나가 확정되어 데모 스킬이 튜토리얼 5종 중 하나와 같아지면 층 구분을 없애고
단일 목록으로 되돌린다.

**미확정 — 결정 필요**
페르소나. D-AI-19 에서 도메인(다품종 소량생산 소형부품)까지는 좁혔지만
"누가 쓰는가"는 아직 없다. 데모 스킬은 여기에 종속된다.


---

## D-AI-26 · 계약에 `skill_id` 를 필수 필드로 추가, 0.2.0 으로 올린다

작성자: 김준태(트랙B)
⚠️ **트랙 A 대행 결정.** 황도경이 FE(MVP 우선순위) 작업 중이라 김준태가 양 트랙을
겸하는 기간의 결정이다. 황도경 복귀 시 이 항목을 먼저 공유하고, 이견이 있으면
그것도 기록 대상이다.

**결정**

```
contract_version  "0.1.0-provisional" → "0.2.0-provisional"
EpisodeMeta       skill_id: str 추가 (contract/ids.py 의 SKILL_IDS enum 강제)
                  task 는 사람이 읽는 자유 문자열로 남긴다
data/collect.py   --skill-id 필수 인자
```

`contract/ids.py` 를 새로 만들어 `SKILL_IDS`·`DESTINATIONS`·`TIERS`·`STATUSES` 를
둔다. 계약과 레지스트리가 서로를 임포트하면 순환이 되므로 공유 어휘는 둘 아래에 둔다.

**왜 지금인가 — 비용이 비대칭이다**

| 언제 바꾸나 | 폐기되는 것 | 비용 |
|---|---|---|
| **지금** | 시뮬 스크립트 98편 (재생성 가능) | 서버 약 20분 |
| UMI 실수집 착수 후 | 실시연 데이터 전량 | 750편, 되돌릴 수 없음 |

실시연 데이터가 **0편**이다. 계약을 바꿀 수 있는 마지막 구간에 가깝다.

**왜 `task` 로 대신하지 않는가**
`task` 는 자유 문자열이라 오타·표기 흔들림을 검증기가 잡지 못한다. 플랫폼·사용자
클릭·VLM 선택·로봇 실행 네 주체가 같은 것을 같은 이름으로 불러야 하는데, 그중
하나는 막지 않으면 여섯 번째 스킬을 지어내는 언어모델이다.

**같이 넣지 않은 것 — 의도적 누락**
`demonstrator`(시연자), `object_size_mm`(대상물 치수). 나중에 필요해질 수 있지만
**지금 쓸 데가 없다.** 쓸 데 없는 필드는 아무 값으로나 채워지고, 그러면 필드는
있는데 믿을 수 없는 상태가 된다. 필요해지는 시점에 버전을 다시 올린다.

**폐기되는 것**
`datasets/sim_pick_v1` (98편, 885.6MB) 과 `checkpoints/bc/bc_sim_pick_v1.pt`.
0.1.0 이라 새 검증기가 거부한다. `sim_pick_v2` 로 재수집한다
(`--skill-id pick_place`). 어제까지의 측정 수치는 **그 조건에서 유효하며**,
재측정 시 계약 버전을 병기한다.

**되돌릴 조건**
황도경이 다른 스키마(예: `skill_id` 대신 계층형 `task_id`)를 제시하면 재논의한다.
실데이터 수집 착수 전까지는 되돌리는 비용이 20분이다.


---

## D-AI-27 · 작업 브랜치를 주제 단위로 다시 판다

작성자: 김준태(트랙B) · 관련 이슈 없음 (저장소 운영)

**배경 — 규칙을 조용히 어기고 있었다**

Git 규칙은 `<타입>/ai/<이슈키>-<슬러그>` 로 작업 브랜치를 파게 돼 있는데,
2026-09-01 서버 세팅 이후 **25건 연속으로 `ai` 에 직접 커밋**했다. 8/27~8/28 에는
지키고 있었고(`feature/ai/S15P21A103-63-mujoco-viewer` 등), 급해지면서 흐지부지됐다.

**결정**

브랜치를 **커밋 단위가 아니라 주제 단위**로 판다.

```
브랜치 1개 = 이슈 1개 = MR 1개, 커밋 5~10건 규모

test/ai/S15P21A103-34-bc-failure-diagnosis
feature/ai/S15P21A103-109-skill-registry
chore/ai/S15P21A103-NNN-repo-tooling
```

- `ai` 직접 커밋은 하지 않는다. 직전 커밋 오타 정정은 `--amend` 로 처리한다
- 저장소 잡일(README, push 스크립트, .gitignore)은 **잡일 전용 이슈 하나**로 모은다.
  이슈키 없는 커밋 4건이 나온 것은 붙일 이슈가 없어서였다
- `ai → dev` 병합은 GitLab MR 로 한다. 로컬 병합 후 push 하면 이력이 남지 않는다
- **MR 링크에 대상 브랜치를 반드시 넣는다.** 빠뜨리면 GitLab 이 기본 브랜치(`master`)를
  대상으로 채워서 `ai` 가 `dev` 를 건너뛰고 올라간다:
  `?merge_request[source_branch]=ai&merge_request[target_branch]=dev`

**왜 커밋 단위가 아니라 주제 단위인가**

작업 브랜치의 목적은 셋이다 — 리뷰 단위 분리, 충돌 방지, 되돌리기.
지금은 한 사람이 양 트랙을 하고 MR 을 리뷰하는 사람이 없어 뒤 둘의 이득이 거의 없다.
남는 것은 **리뷰 단위 분리**이고, 그건 커밋마다 브랜치를 파지 않아도 성립한다.
하루 두세 번 전환이면 오버헤드는 작고 이력은 남는다.

**소급 적용하지 않는다**
`ai` 에 이미 쌓인 25건을 쪼개는 것은 이득보다 손해다. 다음 커밋부터 적용한다.

**되돌릴 조건**
MR 처리 오버헤드가 실제로 하루 작업을 지연시키면 재검토한다.
그때는 "규칙을 지키는 척"이 아니라 규칙을 바꾸고 다시 기록한다.

**함께 정리한 것**
병합이 끝난 옛 작업 브랜치 5개(로컬)·3개(원격)를 삭제했다.
`ai-standalone` 은 개인 미러용 산출물이며 작업 브랜치가 아니다.
