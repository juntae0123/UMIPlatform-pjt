# MEASURE — UMI 왕복 IK 변환 손실 (2026-09-07)

- 이슈 S15P21A103-127 · 작성 김준태(트랙B)
- 확신도 🟢 실행·로그 확인

## 측정 조건 — 조건 없이 인용 금지

```
MuJoCo 3.12.0 · numpy 2.2.6 · Python 3.10.12
씬 sim/mujoco/scenes/pick_place.xml · 설정 configs/so101.yaml
입력 5편 x 90스텝 = 450 pose · seed 0
파지점 grasp.pinch_offset_local (solve_pose_ik 가 조준하는 그 점)
```

⚠️ **실행 환경이 프로젝트 표준이 아니다.** 데스크톱 Cowork VM 의 Python 3.10.12 에
   mujoco 를 따로 깔아 돌렸다. `aiot_ai`(로컬) / `aiot_v100`(서버, Python 3.11.16)
   에서 재실행해 대조해야 한다. 순기구학·역기구학이 Python 마이너 버전에 의존할
   이유는 없지만, 그건 추정이고 대조는 안 했다.

## 무엇을 재는 계측인가 — 그리고 무엇을 재는 게 아닌가

유효한 관절 배치에서 FK 로 EEF pose 를 만들고 IK 로 되돌린다.
그 pose 는 **5자유도 다양체 위에 정확히 놓여 있어 도달 가능성이 구조적으로 보장된다.**

- 재는 것: 좌표계·오프셋·부호 규약이 맞는가, 솔버가 어디까지 내려가는가
- **재지 않는 것: 사람 UMI 궤적의 실행 가능성.** 이 수치를 UMI 성능으로 인용하지 않는다.
  실제 사람 손 pose 는 이 다양체 밖에 있고, 그 수용률은 S15P21A103-113 소관이다

## M1. 솔버 정지 허용오차가 왕복 오차의 천장이었다 🟢

`solve_pose_ik` 기본값으로 먼저 쟀고, 분포가 1.0mm 에 몰려 있었다.

| 조건 | 위치 중앙 | p95 | 최대 | 접근축 중앙 | 미수렴 |
|---|---|---|---|---|---|
| `pos_tol=1e-3` (sim 기본값) | 0.830mm | 0.983 | 0.9989 | 0.309도 | 0/450 |
| `pos_tol=1e-5, axis_tol=0.05도` | **0.0088mm** | 0.0099 | 0.0100 | **0.0035도** | 0/450 |

**해석:** 0.830mm 는 기하가 아니라 조기종료 기준이다. 최대값이 0.9989 로 천장에
붙어 있고 미수렴이 0 이라 반복 여유가 남아 있었다. 1mm 는 변환 예산 5mm 의 20%
이고, 재생 허용오차 ±10mm 의 10% 다 — 아무것도 안 하고 쓰는 값이었다.

조인 뒤에도 값은 여전히 새 허용오차(0.010mm)에 붙어 있다. 더 내릴 수 있지만
0.0088mm 는 예산의 0.18% 라 내릴 이유가 없다.

**`solve_pose_ik` 의 기본값은 바꾸지 않았다.** 그 함수는 시뮬·정책 대화 소유이고
스크립트 수집이 그 기본값으로 검증돼 있다. `tools/umi_mujoco.py` 의 `MujocoIK` 가
인자로 덮고, 그 값을 실행 조건으로 기록한다.

## M2. 계측기가 거짓말했다 — roll 잔차의 180도 대칭 🟢

평행 그리퍼의 턱 축은 **방향이 없다.** 접근축 둘레로 180도 돌리면 두 턱이 자리를
바꿀 뿐 같은 파지다. `roll_residual_deg` 가 `[0,180]` 으로 재고 있어서 멀쩡한
파지를 큰 오차로 보고했다.

| | 접기 전 | 접기 후 `[0,90]` |
|---|---|---|
| roll 잔차 중앙 | 46.78도 | 46.98도 |
| roll 잔차 p95 | 106.59도 | **82.05도** |

중앙값은 거의 안 변했다(46.8 < 90). p95 는 106.6 → 82.0. 접기가 분포를 단순히
`180-x` 로 뒤집는 게 아니라서 사전 추정(73도)은 틀렸다.

## M3. 왕복은 pose 를 복원하지만 관절 배치를 복원하지 않는다 🟢

```
관절각 복원 최대오차 (팔 5축)  2.204 rad = 126.3도
```

위치 오차가 0.0088mm 인데 관절각이 126도 다르다. IK 가 **다른 해 분기**로 수렴한
것이다. 위치 3 + 접근축 2 = 5 구속에 자유관절 5개면 해가 이산집합이고, 같은 파지점·
같은 손가락 방향을 다른 팔 자세로 만들 수 있다.

**따라서 왕복 오차를 "IK 가 시연자의 팔 자세를 복원한다"는 근거로 쓸 수 없다.**

## M4. roll 잔차 47도는 5자유도 한계가 아니다 — **원인은 야코비안의 널 방향이었다** 🟢

> **정정 2026-09-07:** 이 절의 최초 결론("분기 선택 실패")도 틀렸다. M5 가 원인을
> 확정했고 M6 이 해소했다. 아래는 관측 사실만 남긴다.

이번 입력의 6자유도 pose 는 **유효한 관절 배치에서 나왔으므로 전부 달성 가능**했다.
그런데도 roll 잔차 중앙값이 47도다. `wrist_roll` 을 풀이에 열어둔 결과 솔버가
아무 분기로나 수렴했고, 되돌릴 수 있었던 정보를 버렸다.

실물 UMI 는 다르다 — 사람 손 pose 는 다양체 밖이라 roll 이 원리적으로 안 맞는다.
하지만 **이 계측에서의 47도는 원리가 아니라 분기 선택 실패다.**

중요도: 2cm 정육면체는 대칭이라 roll 이 무관하다. 타겟 도메인은
**다품종 소량생산 소형부품**이고 비대칭 부품은 턱이 특정 방향으로 물어야 한다.
`solve_pose_ik` docstring 이 "wrist_roll 은 턱이 열리는 방향을 고르는 유일한
수단이다"라고 명시한다.

## M5. 자유도 배분이 알려진 것과 다르다 — 직접 쟀다 🟢

`wrist_roll` 하나만 흔들고 나머지를 고정했다. 무작위 40자세 x 15각도.

| 흔든 관절 | 파지점 이동 최대 | 접근축 변화 최대 | 턱축 변화 최대 |
|---|---|---|---|
| `wrist_roll` | **0.0000 mm** | **0.000 도** | 88.57 도 |
| `wrist_flex` | 282.06 mm | 176.43 도 | 89.60 도 |
| `elbow_flex` | 551.93 mm | 179.83 도 | 89.83 도 |

`wrist_roll` 은 파지점과 접근축에 **영향이 정확히 0** 이고 턱 방향만 바꾼다.
`grasp.pinch_offset_local = [0, 0, -0.08]` 이 roll 축(그리퍼 로컬 z) 위에 정확히
놓여 있고 접근축이 그 축이기 때문이다.

**따라서 실제 자유도 배분은 이렇다.**

```
pan · lift · elbow_flex · wrist_flex (4개)  →  위치 3 + 접근축 2 = 5구속  (과결정)
wrist_roll (1개)                            →  roll 전담, 완전 독립 제어
```

두 가지가 따라온다.

1. **roll 은 버릴 필요가 없다.** 공짜로 맞출 수 있다
2. **`wrist_roll` 을 풀이의 자유변수에 넣으면 해롭다.** 야코비안에 영향 0 인 열이
   생기고 감쇠 최소자승이 그 널 방향으로 표류한다. **M4 의 47도가 그것이다**

⚠️ 이 분해는 파지점이 roll 축 위에 있다는 데 전적으로 의존한다. **로봇팔 변형
   예정**이므로 그리퍼 형상이 바뀌면 무너진다. 재측정 대상이다.

⚠️ `sim/mujoco/kinematics.py` docstring 은 "위치 3 + 접근방향 2 = 5로 정확히
   결정된다 / 접근축 둘레 회전은 wrist_roll 명령 그대로일 뿐, 나머지 다섯과
   **독립적으로 고를 수 없다**"고 쓰고 있다. 뒷부분이 실측과 다르다.
   그 파일은 시뮬·정책 대화 소유이므로 고치지 않고 "변경 예고"로 전달했다.

## M6. 1회 보정으로 roll 을 정확히 맞춘다 🟢

roll 이 `wrist_roll` 에 선형이고 결합이 0 이므로 탐색도 반복도 필요 없다.
자유 풀이 후 부호 있는 roll 오차를 재서 `wrist_roll` 에 한 번 더하면 끝난다.

부호는 **재서 정했다.** 유도하지 않았다 — `wrist_roll` 은 로컬 +z 둘레로 돌고
접근축은 로컬 -z 라서 어딘가에서 뒤집히고, 어느 쪽인지는 MJCF 축 규약에 달렸다.

| 부호 | roll 잔차 중앙 | p95 | 위치 중앙 |
|---|---|---|---|
| `+1` | 56.0068도 | 88.0767 | 0.0084mm |
| **`-1`** | **0.0000도** | **0.0000** | 0.0084mm |

세 전략 대조 (n=100, 목표 pose 를 접근축 둘레로 δ 만큼 돌려 roll 을 달성 불가로 밀었다):

| δ | 안 | 위치 중앙 | roll 중앙 | 초/스텝 |
|---|---|---|---|---|
| 0 | A 자유 풀이 (현행) | 0.0090mm | 45.80도 | 0.0027 |
| 0 | B `wrist_roll` 고정+반복 | 0.0090mm | 0.00도 | 0.0041 |
| 0 | C `q_init` 시드 스윕 9개 | 0.0090mm | 6.68도 | 0.0244 |
| 60 | B | 0.0090mm | 0.00도 | 0.0042 |
| 60 | C | 0.0090mm | 6.28도 | 0.0247 |

⚠️ **δ 섭동은 전략을 구분하지 못했다.** 예측은 "δ 가 커지면 B 의 위치가 무너진다"
였고 틀렸다. `wrist_roll` 이 위치·축과 결합이 0 이라(M5) δ 는 애초에 위치 문제를
만들지 않는다. 섭동을 잘못 골랐다 — 위치·접근축을 다양체 밖으로 밀어야 했다.
그건 S15P21A103-113 이다.

## M7. 최종 상태 — 왕복이 항등에 가까워졌다 🟢

`MujocoIK(match_roll=True)` 기본값으로 5편 x 90스텝:

| | M1 시점 | 최종 |
|---|---|---|
| 위치 오차 중앙 | 0.0088mm | 0.0088mm |
| 접근축 오차 중앙 | 0.0035도 | 0.0035도 |
| roll 잔차 중앙 / p95 | 46.98 / 82.05도 | **0.000 / 0.000도** |
| **관절각 복원 최대오차** | **126.28도** | **0.10도** |
| 미수렴 · 한계클램프 | 0/450 · 0/450 | 0/450 · 0/450 |

관절각 복원이 0.10도라는 것은 **왕복이 사실상 항등**이라는 뜻이다.
M3 의 "왕복이 관절 배치를 복원하지 않는다"는 해소됐다.

이게 층 1(변환 정확성)의 값을 크게 올린다 — 왕복이 항등이면
**하류에서 나오는 불일치는 분기 모호성이 아니라 진짜 버그다.**

## M8. 층 1 관통 — 완료기준 2개 판정 🟢

`tools/make_umi_synth.py` 로 UMI raw 20편을 만들고 `tools/convert_umi.py` 로
계약 데이터셋 `datasets/umi_synth_v1` 으로 변환했다. **시뮬 데이터셋을 읽지 않았다.**

```
raw            20편 x 90스텝 · 8.3MB (평균 0.41MB)
계약 데이터셋   20편 · 9.3MB (평균 0.47MB) · 평균 89.8스텝
verify_dataset  에피소드 20개, 계약 위반 0개
state 값 범위   실측 [-0.9792, 0.8182]   (계약 [-1, 1])
```

| 완료기준 (결과 보기 전 확정) | 실측 | 판정 |
|---|---|---|
| UMI 형식 입력 20편이 계약 검증기를 위반 0 으로 통과 | 위반 0/20 | **통과** |
| IK 변환 위치 오차 중앙값 5mm 이내 | **0.0087mm** | **통과** |

부산물:

```
스텝 수용률        1797/1800 = 99.8%    폐기 이유 {ik_not_converged: 3}
roll 잔차 중앙     0.0000도
pose↔이미지 동기   최대 8.00ms (계통적)
identity 기준선    mean|action-state| 0.02046 · MSE 7.592e-04
이미지 정렬 대조   프레임번호 불일치 0 · 카메라코드 불일치 0
```

**이미지 정렬 대조가 독립 검사다.** 각 스텝의 이미지 픽셀에 심어둔 프레임 번호와
카메라 코드를 되읽어, 전체 프레임에 대한 **무차별 최근접 탐색**과 대조했다.
`umi.convert._nearest_frames` 를 다시 쓰지 않았다 — 같은 코드로 검사하면 그 코드의
off-by-one 을 못 잡는다.

### M8-a. 미수렴 3건의 원인 확정 — 특이자세가 아니라 **시드 없는 풀이**였다 🟢

> **정정.** 이 절의 최초 기록은 "특이자세로 추정, 확인하지 않았다" 였다.
> 좁혀보니 특이자세는 **기각**이다. 과정은 `TS_umi_ik_unseeded_0907.md`.

```
σmin  성공 1797건  5퍼센타일 0.02370      실패 3건  최대 0.02762
      → 실패의 최악보다 작은 성공 스텝이 242건 (13.47%)  ⇒ 특이자세 기각
참값 시딩  3/3 이 0.0000mm 로 수렴                      ⇒ 시딩 문제 지지
```

실패는 **ep7 의 step 0·1·2 연속**이다. `step 0` 은 유일하게 `q_init` 이 없는
스텝이고, `convert()` 는 실패 시 `q_prev` 를 갱신하지 않으므로 step 1·2 도 계속
원점에서 푼다. 목표가 원점의 수렴 분지로 들어온 step 3 에서 회복한다.

### M8-b. **정정** — 3건은 끝이 아니라 시작이었고, 운이 아니라 계통적이다

최초 기록: "3건이 전부 에피소드 끝에 있었던 것은 운이다". **두 번 틀렸다.**

- 끝이 아니라 **에피소드 시작**의 연속 3스텝이다 (87스텝 남음 → 평균 89.85 와 일치)
- 운이 아니다. **`step 0` 이 유일하게 시드가 없는 스텝**이므로 실패는 거기서
  시작될 수밖에 없다

다만 대가 자체는 그대로다 — **내부에 폐기가 하나만 있어도 에피소드의 절반이
날아간다.** 검증기 실측: n=90, 40번째 스텝 폐기 → 49스텝만 남음(45% 손실).

### M9. 조치 후 — 수용률 100% 🟢

시드 없는 스텝에만 결정적 시드 8개로 다중시작한다 (`MujocoIK._seed_grid`).

| | `umi_synth_v1` (조치 전) | `umi_synth_v2` (조치 후) |
|---|---|---|
| 미수렴 스텝 | 3 / 1800 (0.17%) | **0 / 1800** |
| 스텝 수용률 | 99.8% | **100.0%** |
| 평균 스텝 | 89.8 | **90.0** |
| IK 위치 오차 중앙 | 0.0087mm | 0.0087mm |
| 계약 위반 | 0/20 | 0/20 |

**현행 데이터셋은 `umi_synth_v2` 다.** v1 은 조치 전 기준선으로 남긴다
(이름 재사용 금지 규칙).

⚠️ 실데이터에서는 다중시작으로 해결되지 않는다. 사람 손 pose 는 (위치, 접근축)
자체가 다양체 밖일 수 있고, 그건 초기값 문제가 아니다.

### M8-c. 동기 오차 8.00ms 는 계통적이다

이미지 스트림을 pose 와 같은 30Hz, 지연 8ms 로 합성했다. 모든 스텝에서 정확히
8.00ms 다 — **랜덤이 아니라 상수 편향이다.** hand-eye 오차와 같은 종류이고,
평균으로 지워지지 않는다. 손 0.3m/s 가정 시 2.4mm 🟡.

계약의 `state`/`action` 10ms 게이트는 **이 오차를 보지 못한다.** 둘 다 pose
스트림에서 나오기 때문이다. 그래서 `EpisodeMeta.notes["image_sync_offset_ms"]` 에
실측값을 넣는다 — 계약을 바꾸지 않고 S15P21A103-30 의 계측이 데이터와 함께 이동한다.

## M10. 관절 복원 정확도는 `pos_tol` 에 걸려 있다 — 2026-09-08 🟢

M7 까지는 **pose** 복원만 봤다. 정책이 배우는 것은 **관절각**이므로 그쪽을 따로
재야 한다. `tools/diff_datasets.py` 로 계약 데이터셋을 배열 수준에서 대조했다
(왕복: 계약 → FK → UMI raw → IK → 계약, 이미지는 바이트 재사용).

판정 기준은 **절대값이 아니라 학습 신호 대비 비율**이다. 정책이 배워야 하는 것은
잔차 `action - state` 이므로, 왕복 오차가 그 신호의 몇 %인지가 의미 있는 수치다.
임의의 절대 허용치는 아무것도 판정하지 못한다 — 처음에 1e-3 을 근거 없이 잡았고
그건 판정 기준이 아니었다.

| 관절 | `pos_tol=1e-5` | `pos_tol=1e-7` | 배율 |
|---|---|---|---|
| shoulder_pan | **8.91%** | **0.96%** | 9× |
| wrist_roll | 5.91% | 0.65% | 9× |
| shoulder_lift | 0.70% | 0.01% | 100× |
| elbow_flex | 1.67% | 0.02% | 100× |
| wrist_flex | 0.90% | 0.01% | 100× |
| gripper | **0.00%** | **0.00%** | — |

이미지 바이트 완전 일치 40/40 · 타임스탬프 완전 일치 · 수용률 100% 유지.

그리퍼가 정확히 0 이므로 gap 곡선 역변환은 수학적으로 정확하다 (내 첫 가설은 틀렸다).

**조치: `MujocoIK` 기본 `pos_tol_m` 을 1e-5 → 1e-7 로 올렸다.**
비용 1.23배(1.93s → 2.37s), 미수렴 0/270 불변, 위치 오차 0.000mm.

`axis_tol_deg` 는 0.05 유지. 100배 조여봤지만 수치가 **바이트 단위로 동일**했다 —
`pos_tol=1e-7` 이면 축 오차가 이미 훨씬 아래라 그 조건이 구속하지 않는다.
**"축 축퇴가 pan 오차를 만든다"는 예측은 기각됐다.**

## M11. 남는 오차는 `pan ⇄ roll` 연속 널 방향이다 — 허용오차로 안 없어진다 🟢

pan·roll 만 √ 스케일링(9배)한 이유를 좁혔다.

```
corr(Δpan, Δroll) = -0.9997          ← 정확히 반대로 상쇄
Δpan 최대 3.877e-4 rad · Δroll 최대 3.874e-4 rad   ← 크기 동일
중앙값은 둘 다 0                      ← 대부분 스텝은 완전 일치
|Δpan| 상위 5% 스텝의 접근축 |z| 중앙 0.971  (전체 0.597)
```

**접근축이 베이스 z축과 평행해지는 자세에서 `shoulder_pan` 과 `wrist_roll` 이
같은 선 둘레 회전으로 축퇴한다.** 그 방향으로 (위치, 접근축)이 불변이므로
**연속 1차원 널 방향**이고, 허용오차를 조여도 사라지지 않는다.

크기 0.022도 = 최악 관절·최악 스텝에서 움직임의 0.96%, **중앙값 0%**.
롤아웃 실행 간 변동 25%p 와 identity 잔차 0.0205 에 비하면 잡음 바닥 아래다.

**판단: 받아들이고 기록한다. 더 쫓지 않는다.** 5자유도 팔의 기하 성질이고 버그가
아니다. 널 방향이라는 것은 pan 을 **고를 수 있다**는 뜻이기도 하다 — 직전 해에서
이어가는 것이 이미 그 역할을 하고, 중앙값 0 이 그 증거다.

⚠️ 실물에서 이 자세(수직 접근)를 쓰는 스킬이 있으면 pan 라벨에 이 정도 잡음이
   들어간다. `present_inspect` 처럼 수직 접근이 많은 스킬에서 재확인할 값이다.

## 미검증 / 이 계측이 보장하지 않는 것

| 항목 | 상태 |
|---|---|
| 실물 UMI 궤적의 수용률 | **해당 없음.** 입력이 구조적으로 도달 가능하다 |
| `aiot_ai` / `aiot_v100` 에서의 재현 | **미대조.** 위 실행 환경 경고 참조 |
| gap 곡선의 실물 대응 | 표의 자기일관성만 확인(0.0000mm). 실물 리그 턱과 무관 |
| 이미지 경로 | 이 계측은 이미지를 쓰지 않는다 |
| 학습 적합성 | 해당 없음. 이건 변환 정확성 계측이다 |
| M5 의 자유도 분해 | **현행 그리퍼 형상 전용.** 로봇팔 변형 시 무효 |
| δ 섭동 실험 | 전략 구분에 **실패**. 섭동 축을 잘못 골랐다 (M6 참조) |
| 시드 8개의 충분성 | **미검증.** 이 입력에서 0건이 됐을 뿐이다 |
| 층 1 의 이미지 | 합성 기울기 + 인덱스 비트다. 렌더도 실사도 아니다 |
| 층 2 (학습→롤아웃 완주) | **미실행.** 실제 이미지가 있는 데이터셋이 필요하다 |

## 다음 계측 — 예측을 먼저 적는다

1. **위치·접근축을 다양체 밖으로 미는 섭동.** M6 의 δ(roll 만 회전)는 구분에
   실패했다. `wrist_roll` 이 결합 0 이라 roll 은 늘 맞춰지기 때문이다.
   밀어야 하는 것은 (위치, 접근축) 조합이다 — 4관절에 5구속인 과결정 계.
   **예측: 위치 오차가 0.0088mm 에서 급격히 올라가고 5mm 초과 스텝이 처음 나온다.
   접근축 오차가 위치보다 먼저 무너진다** (`axis_weight=0.15` 로 가중이 낮으므로).
   이게 S15P21A103-113 수용률의 실체다.
2. 표준 환경(`aiot_ai`)에서 M1·M7 재실행, 수치 대조 + `--log` 로 EXP_LOG 등재.
3. **내부 폐기 스텝의 관절 보간.** 최장 연속 구간 규칙은 내부 폐기 1건에
   에피소드 절반을 낸다(M8-b). pose 는 연속이므로 **관절 궤적 보간은 픽셀 보간과
   달리 정당하다** — 다만 보간 결과를 FK 로 되돌려 측정 pose 와 대조하고 예산
   초과면 폐기해야 한다. **예측: 1~2프레임 구멍은 FK 위치 오차 0.5mm 이내로 메워지고,
   5프레임을 넘으면 예산(5mm)을 넘긴다.**
4. **파지점이 roll 축을 벗어났을 때** M5 분해가 어떻게 무너지는지.
   `pinch_offset_local` 의 x·y 를 0 이 아닌 값으로 두고 M5 를 재측정.
   **예측: 오프셋 크기에 비례해 파지점 이동이 생기고, 그 순간 1회 보정이
   부정확해져 반복이 필요해진다.** 로봇팔 변형 전에 알아둬야 하는 값이다.

## 재현

```bash
# [로컬]
cd AI
python tools/check_umi_convert.py                       # 변환기 불변식 26건
python tools/umi_mujoco.py --episodes 5 --steps 90       # 조인 허용오차
python tools/umi_mujoco.py --pos-tol-m 1e-3 --axis-tol-deg 1.0   # sim 기본값 재현
```

코드 리비전  (M8 추가 시점)
