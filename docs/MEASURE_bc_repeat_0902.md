# MEASURE · BC 98편 학습 3회 반복 롤아웃 (n=100 ×3)

작성: 김준태(트랙B) · 2026-09-02 · 관련 이슈 S15P21A103-34, -35 · D-AI-28
환경: V100-PCIE-32GB 서버, MuJoCo 3.12.0, Python 3.11.16, `MUJOCO_GL=egl`,
      git `acdf751` (dirty=True), torch 2.13.0+cu126
데이터: `datasets/sim_pick_v2` — 98편 13,818샘플, 계약 0.2.0, 거부 0
설정: `configs/train/bc.yaml` (action_space **joint_delta_gripper_abs**, 표준화 켬,
      L1, lr 3e-4, batch 32, dropout 0.1, val_fraction 0.2), `--epochs 30`
평가: 물체 xy ±50mm, 시드 3000~3099 (3회 동일), 틱 제한 200, 렌더 켬
명령: `python tools/repeat_runs.py --data datasets/sim_pick_v2 --runs 3 --epochs 30 --episodes 100 --device cuda --log`

**판정: 배포 게이트(20%) 실패. 그러나 이 프로젝트에서 학습 정책이 0 이 아닌 첫 수치.**

## 결과 🟢

| 시드 | val_loss (표준화 단위, baseline 0.766) | 롤아웃 | 평균 상승 | 학습 시간 |
|---|---|---|---|---|
| 0 | 0.0427 (best 0.0400) | **11/100** | 0.56cm | 619초 |
| 1 | 0.0474 (best 0.0449) | **2/100** | 0.10cm | 570초 |
| 2 | 0.0545 (best 0.0468) | **4/100** | 0.20cm | 573초 |
| 평균 | | **5.7%** · 범위 2~11% (폭 9%p) | | |
| 합산 | | **17/300** · Wilson 95% 3.6~8.9% | | |

baseline (3회 동일 시드): hold 0/100 · zero 0/100 · **scripted 84/100** (Wilson 95% 약 75.7~90.0%)

## 읽는 법

- **하한 3.6% > 0**: hold·zero(0%) 와 구분된다. 정책이 무언가를 한다.
- **게이트 20% 미달**: 상한 8.9% 도 못 미친다. 배포 불가.
- **val 은 롤아웃 순서를 예측하지 않았다** — val 최저(seed0)가 롤아웃 최고이긴 하지만
  seed1(0.0474) 2% < seed2(0.0545) 4%. 표본 3개로는 아무 말도 못 한다.
- **실행 간 폭 9%p**: 어제 실측(25%p)보다 좁지만 n=3 이다.
- **scripted 84%** 가 어제 n=20 "95%" 를 대체한다. 어제 값의 CI(77~99%) 안이다.
  ceiling 게이트 80% 를 4%p 차로 통과 — 상한 자체가 낮다는 뜻이기도 하다.

## 이 수치가 말하지 않는 것

- 5.7% 가 어디에서 나오는지 (중앙 근처 집중인지 흩어져 있는지) → `success_map --runs`
- 정책이 이미지를 보는지 → `image_sensitivity` (seed0 ckpt)
- 실패의 형태 (접근 실패 / 잡기 위치 오차 / 그리퍼 타이밍) → 계측기 없음. 필요하면 만든다

## 직전 상태와의 대조

| | 09-01 | 09-02 오후 | **이번** |
|---|---|---|---|
| 행동 공간 | joint_absolute | joint_delta (표준화 누락) | joint_delta_gripper_abs |
| val (단위) | 0.00517 (raw 절대) | 0.00102 (raw 델타) | 0.043 (표준화, baseline 0.766) |
| 롤아웃 | 0% (n=20) | 0% (n=20) | **5.7% (n=300, 3회)** |

세 행의 val 은 단위가 전부 달라 **비교 불가**다. 비교 가능한 것은 롤아웃 행 하나다.
