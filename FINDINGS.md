# FINDINGS — 실험이 실제로 발견한 것

> **이 파일은 `tools/findings.py` 가 `EXP_LOG.jsonl` 에서 생성한다. 손으로 고치지 마라.**
> 해석·가설은 `findings_notes.yaml` 에 쓰면 여기에 합쳐진다.
> 실험을 시작하기 전에 이 파일을 읽는다. 기억으로 진행하지 않는다.

로그 기록 **55건** · 최신 기록 `2026-09-01 08:29`

> ⚠️ **이 로그에 `repeat_runs` 기록이 없다.** 학습·평가는 서버에서 돌므로, 서버의 `EXP_LOG.jsonl` 을 커밋하기 전에는 이 원장이 성적을 못 본다. 기록 없음을 실험 없음으로 읽지 마라.

## 진단 이력

| 시각 | 종류 | 대상 | 결과 |
| --- | --- | --- | --- |
| 2026-09-01 02:17 | 수집 |  | 98편 저장 · 도달불가 2 · 파지 98 · 9.04MB/편 · 지터 0.05 |
| 2026-09-01 08:28 | diagnose | bc_sim_pick_v1.pt | bc L1 0.005138 vs identity 0.007543 · 붕괴아님 True · 자명예측기초과 False |

## 기각된 가설

- **R-AI-12 dwell (파지 후 정지 구간)** — 파지 직후 dwell_s 0.5초를 넣으면 파지가 안정된다는 가설
  - 근거: v4 dwell 0.5s 0/300 (CI 0~1.3%) vs v5 dwell 0 28/300 (CI 6.5~13.2%). 구간 안 겹침 🟢
  - 되살릴 조건: 행동 표현이 관절 절대각에서 델타/청크로 바뀌면 다시 볼 것
- **R-AI-13 시연 타이밍 변주** — 시연 속도를 섞으면 일반화가 는다는 가설
  - 근거: 프로젝트 원칙 '환경은 다양하게, 시연 방식은 동일하게'. 미측정 상태로 기각 🟡
  - 되살릴 조건: 동일 시연으로 20% 게이트를 넘긴 뒤에만
- **가설 A — 도달 범위 부족** — 물체가 팔이 못 닿는 곳에 있어서 실패한다
  - 근거: Center 0~15mm 구간 정밀도 최고(5.8mm)인데 성공률 0%. 가장 쉬운 곳에서도 실패 🟢
  - 되살릴 조건: 물체 배치 범위를 바꾸면 재측정
- **가설 D — 데이터 밀도 부족** — 특정 위치의 시연이 적어서 그 위치를 못 배운다
  - 근거: 위와 같은 측정. 시연이 가장 조밀한 중심부 성공률 0% 🟢
  - 되살릴 조건: 동일
- **가설 — 정책이 이미지를 안 본다** — 이미지를 무시하고 state 만 회귀해서 실패한다
  - 근거: image_sensitivity 델타비율 0.740 >= 0.30 통과 🟢
  - 되살릴 조건: 인코더를 바꾸면 재측정
- **가설 — 정책이 붕괴(mode collapse)했다** — 출력이 평균으로 뭉개져 아무 동작도 안 만든다
  - 근거: diagnose_bc: bc L1 0.001620 vs identity 0.007543, std 비율 0.970~1.001 🟢
  - 되살릴 조건: 손실 함수를 바꾸면 재측정
- **가설 — lift 구간 신호가 약하다** — 들어올림 구간의 학습 신호가 다른 구간보다 약해서 못 배운다
  - 근거: 구간별 델타 비교에서 lift 신호가 다른 구간과 비슷 🟢
  - 되살릴 조건: 행동 표현 변경 시

## 살아있는 가설 / 다음 후보

- **누적 오차(compounding error) — 현재 최우선 설명** — A(재생) 통과 · B(교사강요) 통과 · C(폐루프) 실패 = 공변량 이동의 교과서적 서명
  - 근거: trace_execution: C 에서 state 차이가 t=1 부터 약 10틱마다 2배. 이미지 차이 0.28→8.53 계조 🟢
  - 다음: DAgger — 시뮬에 84% scripted 전문가가 있으므로 라벨 비용이 0에 가깝다
- **이미지 민감도는 취약점이 아니라 교정 채널이다 🟡** — 작은 이미지 변화를 무시하는 정책은 벗어난 상태를 되돌릴 수 없다
  - 근거: v6 잡음증강: B 비율 0.175→0.1313 로 개선됐는데 C 는 성공→실패로 뒤집힘. seed0 12/100 vs v5 14/100 🟢
  - 다음: v6 seed1·seed2 완료 후 판정. 확정되면 L39 를 '취약점'에서 '채널'로 정정하고 증강을 R-AI-14 로 기각
- **action chunking / ACT** — 한 스텝씩 회귀하면 스텝마다 오차가 쌓인다. 묶어서 내면 누적 지점이 줄어든다
  - 근거: 미측정 🟡
  - 다음: DAgger 다음. baseline 없이 넘어가지 않는다는 원칙에 따라 BC 가 게이트를 넘거나 원인이 확정된 뒤
- **행동 표현 — 절대 관절각의 정밀도 은폐** — 손실 대부분이 '팔이 지금 어디 있나'로 채워지고 잔차(action-state)는 틀린 채 남는다
  - 근거: identity 대비 L1 은 낮으나(0.00162 vs 0.00754) 폐루프는 실패 🟢
  - 다음: joint_delta_gripper_abs 표현으로 동일 조건 비교

## 계측기 상태

- **closing_moment (eval/rollout.py)** — 고쳤다. 히스테리시스 5틱 + 진폭 하위 25% 기준
  - 근거: 이전 판정은 그리퍼 명령이 2단계라는 전제였는데 학습 정책은 연속 회귀다. 판정 60틱 vs 실제 102틱 🟢
  - 되살릴 조건: 판정 문구도 단정을 뺐다. 이 값으로 원인을 정하지 않는다
- **렌더 결정론** — L37 해소. offsamples 0 (MSAA off) 으로 비트 단위 동일
  - 근거: check_determinism 통과 🟢
- **scripted 상한선** — 5회 연속 84.0%. 계측기가 표류하지 않는다
  - 근거: 동시에 시연자 통일이 궤적을 바꾸지 않았다는 증거이기도 하다 🟢
- **조건 표류 검출 (run_conditions)** — 조건 dict 를 만드는 곳을 한 군데로 모았다
  - 근거: 첫 실행에서 image_noise_gray·tag 가 바뀌었는데 '동일하다'고 보고했다. 조건을 두 군데서 따로 만든 탓 🟢
- **n 의 한계** — n=20, p≈0.8 의 구간 반폭(±18%p)이 게이트 폭(20%p)과 같다
  - 근거: 게이트를 옮기지 말고 n 을 올린다. 게이트 변경은 사후 합리화다

