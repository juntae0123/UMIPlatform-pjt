# REJECTED — 안 만들기로 한 것

**안 만든 것도 결정이다.** 기각된 아이디어를 지우면 같은 논의를 몇 주 뒤에 다시 한다.
각 항목에 **되살릴 조건**을 적는다. 조건이 충족되면 다시 꺼낸다.

번호는 `R-AI-{n}`.

---

## R-AI-1 · 개인 사용자 맞춤 그리퍼 학습 플랫폼

- 제안: 김준태(트랙 B) · 기각: 2026-08-31 · 기각 판단: 김준태
- **기각 근거.** 도메인이 아니라 또 하나의 플랫폼이다. "어떤 개인이 무슨 작업을 하는가"에 답하지 못한다.
  그리고 그리퍼가 사용자마다 다르면 우리 계측 전부(도달영역 15×35cm, 파지 가능성, 볼록껍질 관통,
  위치 허용오차 ±10mm)가 **이 그리퍼 하나에 묶여 있어서** 사용자마다 재측정이 필요하다 🟢.
  데이터 분포를 좁히는 게 아니라 넓히는 방향이다. 팔이 한 대이므로 6주에 그리퍼 2종 증명 불가.
- **되살릴 조건.** 단일 그리퍼로 첫 태스크 롤아웃 성공률이 게이트를 통과한 뒤,
  "그리퍼를 바꿔 끼우면 새 작업을 배운다"를 Stretch 데모로.

## R-AI-2 · 보조 홀딩 (화가 팔레트 들어주기 류)

- 제안: 김준태(트랙 B) · 기각: 2026-08-31
- **기각 근거 둘.**
  ① **들고 있기는 학습이 필요 없다.** 자세 하나로 가서 정지하는 것이고, 그건 우리 `hold` baseline 이다.
     마찰 클램프·매직암이 훨씬 싸게 더 잘 한다. 우리 기술이 개입할 자리가 없다.
  ② **무게.** 물감 얹은 팔레트는 수백 g 으로 추정되고 🟡, 우리 팔 **총질량이 0.632kg** 이다 🟢.
- **살아남은 것.** "시장 크기가 1인 작업" 이라는 관점 자체는 유효하고, 이것이 도메인을
  고객군이 아니라 **작업의 모양**으로 정의하게 만든 계기다 → D-AI-11.
- **되살릴 조건.** 페이로드가 수백 g 이상인 팔로 교체되고, 정적 유지가 아니라
  "건네주기·바꿔주기" 처럼 동작이 있는 작업으로 재정의될 때.

## R-AI-3 · 공장 생산라인 타겟

- 제안: 팀 초기 가정 · 기각: 2026-08-31 · 지적: 김준태
- **기각 근거.** 대량생산은 작업이 고정이고 반복이 수백만 번이라 SI 프로그래밍 비용이 물량에 희석된다.
  정밀도 요구가 ±10mm 보다 훨씬 빡세고, 페이로드가 수십 g 을 훨씬 넘고, 다운타임 비용 때문에
  학습 정책보다 결정론적 재생을 원한다. **작업이 고정이고 물량이 많으면 모방학습의 가치는 0이다.**
- **대체된 것.** 앞 공정이 사람인 자리(물체 위치가 매번 다른 자리)로 타겟을 옮겼다 → D-AI-11.
- **되살릴 조건.** 없음. 이 하드웨어로는 되살릴 수 없다.

## R-AI-4 · VLM 을 MVP 에 포함

- 제안: 프로젝트 초기 스코프 · 기각: 2026-08-31 (MVP 범위에서만)
- **기각 근거.** 판정 기준을 먼저 정했다 — **"VLM 이 없으면 못 하는 일"을 한 문장으로 못 쓰면 뺀다.**
  스킬을 목록에서 클릭으로 고르는 구조에서는 드롭다운이 자연어보다 낫다. 8GB 예산이 통째로 남는다.
- **되살릴 조건 (둘 중 하나).**
  ① 스킬 수가 목록으로 고르기 어려울 만큼 많아질 때
  ② 스킬 **안에서 대상을 지목**해야 할 때 — "이 중에 긁힘 있는 것만" (검사 분류 스킬의 자연스러운 확장)
  되살릴 때 이슈 42(Jetson 8GB 검증)를 먼저 통과해야 한다.

## R-AI-5 · 단일 멀티태스크 모델 (스킬 조건부 입력)

- 제안: 김준태(트랙 B) 검토안 B · 기각: 2026-08-31 (MVP 범위에서만)
- **기각 근거.** 데이터 효율은 좋지만 **회귀 위험**이 크다. 스킬을 하나 추가·수정하면 전체를 재학습해야 하고
  기존 스킬 성능이 조용히 떨어질 수 있다. 그걸 확인하려면 스킬 전부를 재측정해야 한다.
  발표 6주 전 일정에서 그 운영 비용은 받을 수 없다.
  우리 정책이 1,307,974 파라미터 ≈ 5MB 라 🟢 스킬별 독립 체크포인트를 수십 개 올려도 메모리 문제가 없다 —
  멀티태스크의 유일한 장점이 우리 경우엔 장점이 아니다.
- **되살릴 조건.** 스킬 수가 늘어 스킬당 수집 비용이 병목이 될 때. 로드맵 스토리로는 유지한다.

## R-AI-6 · MVP 학습 스킬을 2개로 제한

- 제안: **Claude(어시스턴트)** · 기각: 김준태 · 2026-08-31
- **기각 근거.** 제안의 전제가 틀렸다. "모든 스킬이 학습 정책"이라고 가정했기 때문에 2개가 한계로 나왔다.
  실제로는 `Policy` 프로토콜이 학습 정책과 스크립트를 구분하지 않고, 롤아웃 harness 가 둘을
  같은 조건으로 채점한다 🟢. 위치 변주가 없는 작업은 스크립트로 충분하다.
  또한 시뮬에서 스크립트 수집을 하면 사람 시간이 0이므로 학습 스킬 개수 제한이 완화된다.
- **남은 제약 (기각되지 않음).** 학습 스킬이 최소 1개는 있어야 하고, 그 하나는
  replay 와 나란히 세워 이겨야 한다. 스크립트 스킬만 있으면 티칭 펜던트와 구별 불가다.

## R-AI-7 · 절대 관절각을 회귀 목표로 쓰기 (`joint_absolute`)

- 제안: 초기 구현 기본값 · 기각: 2026-09-02 · 판단: 김준태(트랙 B)
- **기각 근거.** 실측 🟢 — 절대 목표에서는 **상태만으로 정답의 86% 가 설명된다**
  (`image_sensitivity`, 09-02). 물체가 옮겨지면 팔도 다른 곳에 있고 그 변위가 곧 정답 차이다.
  신경망이 이미지를 볼 이유가 없어지고, 실제로 움직임을 만드는 잔차 `action − state` 는
  작은 채로 틀리게 남는다. 이미지 기여도 0.185 로 게이트(0.30) 미달.
  잔차로 바꾸자 팔 5관절 이미지 기여도가 0.68~0.96 으로 올라갔다 🟢.
- **되살릴 조건.** 없음 — 다만 **계약은 절대 관절각 저장을 유지한다.** 이건 학습·추론 시점의
  변환이지 저장 포맷 결정이 아니다. `ACTION_SPACES` 에 선택지로 남겨 재측정은 가능하다.

## R-AI-8 · `best val_loss` 로 체크포인트 선택

- 제안: 초기 구현 · 기각: 2026-09-02 · 판단: 김준태(트랙 B)
- **기각 근거.** 이 프로젝트 규칙이 "val loss 는 학습이 망가졌나만 말한다" 인데
  정작 체크포인트 선택을 그것이 하고 있었다. 실측 🟢 — val 곡선이 평평한 **200 epoch 실행이
  epoch 1 모델을 저장**했고, "정책을 평가"한 롤아웃이 학습되지 않은 신경망을 평가했다.
  주 체크포인트는 **마지막 epoch**, best-val 은 `_bestval.pt` 로 참고용 분리 (커밋 f828373).
- **되살릴 조건.** val 이 롤아웃 성공률과 상관을 보인다는 것을 **측정으로 보인 뒤**.
  - sample 분할 시절: 반례 🟢 (val 0.040 / 0.045 / 0.047 → 롤아웃 11 / 2 / 4%, 순서 불일치)
  - **episode 분할 (2026-09-03) 🟢: 처음으로 순서 일치** (val 0.05333 / 0.05721 / 0.06939 →
    롤아웃 9 / 3 / 1%, 3/3). 최종 epoch val 로도 같은 순서.
    ⚠️ **n=3, 우연 확률 1/6 ≈ 17% 로 아직 부족하다.** 학습 6회 이상에서 순위상관을 재야 한다

## R-AI-9 · sample(틱) 단위 val 분할

- 제안: 초기 구현 · 기각: 2026-09-03 · 판단: 김준태(트랙 B)
- **기각 근거.** 같은 에피소드의 프레임이 train·val 양쪽에 들어간다. val 프레임은 같은 물체
  위치·같은 궤적의 학습 프레임에서 몇 ms 떨어져 있을 뿐이라, **본 에피소드 안의 보간을 재는 것이지
  처음 보는 물체를 다루는지를 재지 않는다.** 실측 🟢 — val 0.043 (자명한 예측기 0.766 의 5.6%)
  인데 롤아웃 5.7%. 기본값을 `val_split: episode` 로 바꿨다 (커밋 ad4907e).
- **되살릴 조건.** 에피소드 수가 너무 적어 통째 홀드아웃이 불가능할 때. 그때도 수치에
  "일반화를 재지 않음"을 병기한다. `--split-by sample` 로 여전히 선택 가능하다.

## R-AI-10 · `scripted` 를 물리 외란 프로브로 사용

- 제안: **Claude(어시스턴트)** · 기각: 2026-09-01 · 판단: 김준태(트랙 B)
- **기각 근거.** 설계 오류였다. 도메인 랜덤화 A/B 에서 `scripted` 가 두 번 다 정확히 70.0% 를
  냈는데 🟢, 원인은 "외란에 강해서"가 아니라 **실패가 전부 IK 도달 불가(기하 문제)라서**였다.
  물체 위치를 특권 정보로 읽는 정책은 조명·마찰·카메라 외란을 애초에 통과하지 않는다.
  외란 프로브로 쓰면 붕괴율이 항상 0 으로 나오고 결론은 "전이가 잘 된다"가 된다 — 가장 비싼 오답.
  `--skip-scripted` 를 추가했다.
- **되살릴 조건.** 없음. 상한선(ceiling) 측정 용도로만 유지한다.

## R-AI-11 · 대형 VLM 을 파인튜닝하고 경량 모델을 배포

- 제안: 팀 논의 · 기각: 2026-09-03 · 판단: 김준태(트랙 B)
- **기각 근거.** 성립하지 않는다 — **큰 모델을 파인튜닝해도 작은 모델이 그 가중치를 물려받지 않는다.**
  가능한 건 (a) 큰 모델을 라벨러로 쓰고 그 라벨로 작은 모델 학습, (b) 로짓 증류 둘뿐이다.
  (a) 는 **시뮬 데이터에 무의미하다** — 시뮬레이터가 정답을 이미 안다 (D-AI-17).
  (b) 는 같은 계열·토크나이저가 필요하고, 우리 출력은 스킬 5종 × 목적지 5종 enum 이라
  증류할 복잡도가 없다. 실측상 SmolVLM2-500M 의 fp16 게이트 여유가 Qwen2.5-VL-3B 보다
  7배 크다 🟢 (0.9961 / 0.221 vs 0.9909 / 24.558).
- **되살릴 조건.** **실물 사진에 라벨을 붙일 때** — 시뮬과 달리 정답이 없으므로 (a) 가 의미를 갖는다.
  그때도 게이트는 "작은 모델의 enum 정확도가 큰 모델과 얼마나 일치하나"이지,
  큰 모델을 썼다는 사실 자체는 근거가 아니다.
