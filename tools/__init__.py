"""Runnable entry points. Thin shells — logic lives in the packages.
실행 진입점. 얇은 셸이고 로직은 패키지 안에 있다.

Run from the AI/ directory:
AI/ 디렉터리에서 실행한다:

    python tools/grasp_check.py --trials 20
    python -m tools.grasp_check --trials 20    # 같은 것
"""
